# Lamoon HR — Web (placeholder)

Not scaffolded yet. When UI work starts (V1 ATS screens), generate here:

```bash
# from apps/
npx create-next-app@latest web --ts --tailwind --app --eslint
# then: shadcn init, React Query, Zustand, a typed client for /api/v1
```

Kept as a placeholder so the monorepo layout and docker-compose slot are
reserved. ponytail: no point hand-writing Next boilerplate a generator emits.
